# Results

- `logs/` — raw simulation run logs (gitignored)
- `coverage/` — coverage databases (gitignored — binary, tool-specific)
- `reports/` — generated, human-readable summaries (regression pass/fail
  tables, coverage % snapshots) — **these are versioned**, since they're
  small, readable, and tell the project's progress story over time
